package com.nom.graalnom.runtime.opcodes.binOp;

public class AndOpCode extends BinOpCode{
    public AndOpCode(int leftRegIdx, int rightRegIdx, int regIdx) {
        super(leftRegIdx, rightRegIdx, regIdx);
    }
}
