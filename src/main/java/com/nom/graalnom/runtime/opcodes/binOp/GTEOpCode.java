package com.nom.graalnom.runtime.opcodes.binOp;

public class GTEOpCode extends BinOpCode{
    public GTEOpCode(int leftRegIdx, int rightRegIdx, int regIdx) {
        super(leftRegIdx, rightRegIdx, regIdx);
    }
}
