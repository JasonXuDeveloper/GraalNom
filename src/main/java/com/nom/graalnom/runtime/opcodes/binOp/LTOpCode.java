package com.nom.graalnom.runtime.opcodes.binOp;

public class LTOpCode extends BinOpCode{
    public LTOpCode(int leftRegIdx, int rightRegIdx, int regIdx) {
        super(leftRegIdx, rightRegIdx, regIdx);
    }
}
