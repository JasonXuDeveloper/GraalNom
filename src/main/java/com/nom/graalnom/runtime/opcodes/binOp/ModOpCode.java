package com.nom.graalnom.runtime.opcodes.binOp;

public class ModOpCode extends BinOpCode{
    public ModOpCode(int leftRegIdx, int rightRegIdx, int regIdx) {
        super(leftRegIdx, rightRegIdx, regIdx);
    }
}
