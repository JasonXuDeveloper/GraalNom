package com.nom.graalnom.runtime.opcodes.binOp;

public class AddOpCode extends BinOpCode{
    public AddOpCode(int leftRegIdx, int rightRegIdx, int regIdx) {
        super(leftRegIdx, rightRegIdx, regIdx);
    }
}
