package com.nom.graalnom.runtime.opcodes.binOp;

public class EQOpCode extends BinOpCode{
    public EQOpCode(int leftRegIdx, int rightRegIdx, int regIdx) {
        super(leftRegIdx, rightRegIdx, regIdx);
    }
}
