package com.nom.graalnom.runtime.opcodes.binOp;

public class DivOpCode extends BinOpCode{
    public DivOpCode(int leftRegIdx, int rightRegIdx, int regIdx) {
        super(leftRegIdx, rightRegIdx, regIdx);
    }
}
