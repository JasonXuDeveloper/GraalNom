package com.nom.graalnom.runtime.opcodes.binOp;

public class GTOpCode extends BinOpCode{
    public GTOpCode(int leftRegIdx, int rightRegIdx, int regIdx) {
        super(leftRegIdx, rightRegIdx, regIdx);
    }
}
