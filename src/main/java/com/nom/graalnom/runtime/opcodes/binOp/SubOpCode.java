package com.nom.graalnom.runtime.opcodes.binOp;

public class SubOpCode extends BinOpCode{
    public SubOpCode(int leftRegIdx, int rightRegIdx, int regIdx) {
        super(leftRegIdx, rightRegIdx, regIdx);
    }
}
