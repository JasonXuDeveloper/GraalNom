class MiniTest
{
	public static fun Main() : Void
	{
		Int a = 10;
		if (a * a > 10)
		{
		    "Correct".Print();
        }
        else
        {
            "Incorrect".Print();
        }
        while (a > 0)
        {
            a = a - 1;
            a.ToString().Print();
        }
	}
}